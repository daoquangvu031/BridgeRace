using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class L_CharacterBrick : L_ObjectColor
{
    //public int countBrick = 0;
    //public void AddBrick(Collider other)
    //{
    //    GameObject newBrick = Instantiate();
    //    newBrick.transform.SetParent(transform.parent);
    //    newBrick.transform.localPosition = new Vector3(0, 0.3f * (countBrick + 3), -0.7f);
    //    newBrick.transform.localRotation = Quaternion.Euler(new Vector3(0, 0, 0));
    //    newBrick.GetComponent<Brick>().SetBrickColor(characterColor);
    //    countBrick++;
    //    stack.Push(newBrick); // Thêm gạch vào stack
    //}
}
